---
title: "07. 流式响应与思考模式"
description: "Streaming 处理管道、Extended Thinking 集成、终端 UI 实时渲染"
outline: deep
---

# 07. 流式响应与思考模式

## 流式响应处理

QueryEngine 负责处理来自 Anthropic API 的流式响应，用户可以实时看到模型生成的内容，而不是等待完整响应返回。

这种设计对于终端交互体验至关重要，让用户能够即时感知 Agent 的"思考过程"。流式处理还与终端 UI 组件（React+Ink）紧密集成，实现实时渲染。

## Extended Thinking

QueryEngine 支持 Claude 的扩展思考模式，允许模型在执行工具之前进行更深层的推理。

这是 Harness 为 Agent 提供的**认知增强**机制，使 Agent 能够在复杂任务中进行更周密的规划。

## 技术要点

- SSE (Server-Sent Events) 流式解析
- 增量渲染与终端清屏策略
- 思考块（thinking block）与工具调用块的交错处理
